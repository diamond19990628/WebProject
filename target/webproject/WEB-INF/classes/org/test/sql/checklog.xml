<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="org.test.dao.checklogDao">
    <!-- resultType为返回类型 -->
	<select id="checklog" parameterType="org.test.entity.Admin" resultType="org.test.entity.Admin" >
		select * from useradmini where username=#{username} and password = #{password}
	</select>
	
	<select id="checklevel" parameterType="org.test.entity.Admin" resultType="java.lang.String" >
		select leve from useradmini where username = #{username} and password = #{password}
	</select>
	
	<update id="changestatus" parameterType="java.lang.String">
		update useradmini set status = '0' where username = #{0}
	</update>
	
	<select id="StatusRead" parameterType="org.test.entity.Admin" resultType="java.lang.String">
		select status from useradmini where username = #{username} and password = #{password}
	</select>
	
	<select id="getAdmin" parameterType="java.lang.String" resultType="org.test.entity.Admin">
		select * from useradmini where username = #{username}
	</select>
</mapper>